name: reader_epub_repair
description: Conservative EPUB repair and explicit Sigil-inspired standardization.
publish_to: none
version: 0.1.0
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  archive: ^4.0.7
  html: ^0.15.6
  path: ^1.9.1
  xml: ^6.5.0

dev_dependencies:
  test: ^1.25.15
