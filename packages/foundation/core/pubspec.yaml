name: core
description: Common utilities, errors, result types, logging interfaces
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  meta: ^1.12.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.2
