name: foundation_domain
description: Domain models and ports for reader platform
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  foundation_core:
    path: ../core


