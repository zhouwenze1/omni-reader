name: domain
description: Domain models and ports (repository/service interfaces)
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  core:
    path: ../core
  meta: ^1.12.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.2
