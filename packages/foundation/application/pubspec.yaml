name: application
description: Use cases and orchestration (no Flutter/plugin deps)
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  core:
    path: ../core
  domain:
    path: ../domain

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.2
