name: foundation_application
description: Application services/usecases for reader
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.8.1
  foundation_domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter


