name: engine_pdf
description: PDF reader engine backed by pdfrx(PDFium) — page-based reading like comics.
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  kernel:
    path: ../kernel
  foundation_domain:
    path: ../../foundation/domain
  path: ^1.9.1
  pdfrx: 2.2.24


