name: services_sync
description: Reading progress sync between devices via the self-hosted sync server.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  foundation_domain:
    path: ../../foundation/domain
  crypto: ^3.0.6

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
