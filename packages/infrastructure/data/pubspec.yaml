name: infrastructure_data
description: File, drift, and hive data implementations
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  foundation_core:
    path: ../../foundation/core
  foundation_domain:
    path: ../../foundation/domain
  engine_epub:
    path: ../../engines/epub
  archive: ^3.6.1
  collection: ^1.18.0
  crypto: ^3.0.6
  drift: ^2.20.3
  hive: ^2.2.3
  path: ^1.9.1
  path_provider: ^2.1.5
  sqlite3_flutter_libs: ^0.5.30


