name: services_search
description: Feasibility package for EPUB full-book search with on-demand indexing.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.5.0 <4.0.0"

resolution: workspace

dependencies:
  archive: ^4.0.7
  reader_parser_core:
    path: ../../../../reader_parser_next/packages/reader_parser_core
  reader_parser_epub:
    path: ../../../../reader_parser_next/packages/reader_parser_epub

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.8
